const mongoose = require('mongoose');


const userSchema = new mongoose.Schema({
    name:{
        type:String
    }
    pass
    gmail

});

const userModule = mongoose.model('testData', testSchema);
// key / refers / to dataBase

module.exports = userModule